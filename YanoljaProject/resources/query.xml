<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="getUserNames">
		SELECT USERID
		  FROM TB_MEMBER
	</entry>
	<entry key="signUp">
		INSERT
		  INTO TB_MEMBER
		  VALUES
		  		(
		  		   SEQ_MEMBER.NEXTVAL
		  		 , ?
		  		 , ?
		  		 , ?
		  		 , ?
		  		)
	</entry>
<entry key = "login">
SELECT RESERVATION_NO
FROM TB_MEMBER
WHERE USERID = ? AND USERPWD = ?
</entry>

<entry key = "allSelect">
SELECT *
FROM TB_ROOM
</entry>

<entry key = "hotelChoice">
SELECT ROOMNO,ROOMNAME,CAPACITY,TYPE,LOCATION,PRICE,REVIEWNO,REVIEW,RATED
FROM TB_ROOM
LEFT JOIN TB_REVIEW USING (ROOMNO)
WHERE ROOMNO = ?
GROUP BY ROOMNO,ROOMNAME,CAPACITY,TYPE,LOCATION,PRICE,REVIEWNO,REVIEW
</entry>
</properties>
